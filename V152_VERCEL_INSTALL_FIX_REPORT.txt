V152 — Vercel Install Fix

Problem seen in Vercel:
- Deployment failed before build during `npm install`.
- Vercel showed npm's internal error: `Exit handler never called!`.

Fixes applied:
- Removed package-lock.json from the deployable ZIP.
- The old lockfile was generated inside the build sandbox and contained registry/cache resolution data that should not be shipped to Vercel.
- Changed Vercel install command to:
  npm install --no-package-lock --no-audit --no-fund
- This forces Vercel to resolve dependencies cleanly from package.json instead of using the broken/stale lockfile.
- Kept Node 24.x in package.json engines.
- Kept the v151 phone-seat cardgame stabilization code unchanged except deployment config cleanup.

Validation:
- npm install completed locally with the new install command.
- npx tsc --noEmit passed.
- Next build completed successfully.
