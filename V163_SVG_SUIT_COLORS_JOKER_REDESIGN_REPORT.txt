V163 — SVG SUIT COLORS + JOKER REDESIGN

Changes made
- SVG card suits now use real playing-card colors:
  - Spades: black
  - Clubs: black
  - Hearts: red
  - Diamonds: red
- Rank/card symbol color now follows the suit color for normal cards.
- Removed the blue-only suit rendering from SVG cards.
- Removed the extra small watermark suit from normal cards so the card face stays cleaner.
- Redesigned the joker card SVG:
  - Keeps the same card frame as the rest of the deck
  - Uses a compact JKR corner label
  - Uses a centered red/black joker emblem instead of the broken star layout
  - Keeps a clear JOKER wordmark
  - Shows assigned joker value as a small red/black indicator only when needed
- Kept all existing card game logic unchanged.

Validation
- npm install --no-package-lock --no-audit --no-fund: passed
- npx tsc --noEmit: passed
- NEXT_PRIVATE_BUILD_WORKER=1 npm run build: passed completely
