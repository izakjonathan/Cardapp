V156 — FULLSCREEN CARD UI + VISUAL CARD FACE POLISH

Changes made
- Updated the playable card UI so hand cards look more like real playing cards:
  - top-left rank/suit
  - centered suit / joker face
  - mirrored bottom-right rank/suit
  - joker cards display as wild cards visually
- Added a compact scoreboard layout while a playable card round is active.
- Compressed the full in-round layout so it fits on screen without page scrolling.
- Switched the active card-table screen to a fixed full-screen layout with no vertical app scroll.
- Reduced vertical spacing, button heights, and section heights during playable mode.
- Made the card action buttons more compact.
- Made the rounds summary card compact during playable mode.
- Kept horizontal hand scrolling for larger hands.
- Kept meld list internally scrollable if many melds accumulate.

Validation
- npm install --no-package-lock --no-audit --no-fund: passed
- npx tsc --noEmit: passed
- NEXT_PRIVATE_BUILD_WORKER=1 npm run build: compiled and generated static pages successfully in sandbox before timeout after completion output
