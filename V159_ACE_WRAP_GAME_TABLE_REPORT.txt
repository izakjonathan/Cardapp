V159 — ACE WRAP RUNS + GAME TABLE UI + NORMAL JOKER FORMAT

Rules changed
- Runs now treat Aces as both low and high in a cyclic sequence.
- Valid run examples now include:
  - A 2 3
  - Q K A
  - Q K A 2
  - K A 2
- Joker-assisted runs use the same cyclic Ace logic.
- Lay-off logic for run melds now also understands the cyclic Ace rule.

UI changed
- Joker cards are back in the same visual structure as the other cards:
  - corner label
  - center symbol
  - mirrored bottom corner
- Active gameplay now has a more game-table / felt style.
- Deck/discard/full pile are presented more like table cards.
- The giant empty meld area was reduced into a compact table meld area.
- The hand keeps the two-row layout from V158.
- Play buttons are kept small and compact.

Validation
- npm install --no-package-lock --no-audit --no-fund: passed
- npx tsc --noEmit: passed
- NEXT_PRIVATE_BUILD_WORKER=1 npm run build: compiled successfully and reached static page generation before sandbox timeout
