V155 — Closing-only round scoring

Built from: rummy500-v154-cardgame-ui-polish.zip

Changes:
- Removed the bottom manual scoring section with player inputs, quick points, closer toggle, and Add round.
- Removed the manual End round button from the playable card game controls.
- A card round now only scores when a player legally closes by discarding the last card in their hand to the discard pile.
- The closing player still receives the existing +15 closing bonus through closedBy scoring.
- Whole discard pile -50 penalties are now held inside the active card round and included only when the round closes, instead of being added as a separate manual/penalty round immediately.
- Updated round-over message to make it clear the closer closed by discarding their last card.
- Reset/rematch/new game handling clears active card round state where appropriate.

Validation:
- npm install --no-package-lock --no-audit --no-fund completed locally. The sandbox Node version is 22, so npm warns because the app is pinned to Node 24.x for Vercel.
- npx tsc --noEmit passed.
- NEXT_PRIVATE_BUILD_WORKER=1 npm run build passed successfully.
